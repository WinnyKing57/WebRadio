#!/usr/bin/env sh
#
# Gradle start-up script for UN*X
# https://services.gradle.org/distributions/
# (cut-down version for GitHub Actions)

DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$DIR/gradle/wrapper/gradle-wrapper.jar" "$@"